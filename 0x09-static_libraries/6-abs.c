#include "main.h"

/**
 * _abs - computes absolute value of an integer
 * @n: character to be checked
 * Return: Always 0
 */

int _abs(int n)
{
	if (n > 0)
	{
		n = n;
	}
	else
	{
		n = n * -1;
	}
	return (n);
}
